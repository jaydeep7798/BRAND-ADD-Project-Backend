spring:
  application:
    name: SBMS-common-DTO
  boot :
    admin :
       client :
           url : http://localhost:1111/
eureka :
    client :
       service-url :
          defaultzone :  http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
     port: 8085
     
